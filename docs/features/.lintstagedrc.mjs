// Diese Seiten werden erzeugt (`pnpm docs:features` bzw. `pnpm docs:api`) und
// dürfen nicht nachformatiert werden — sonst meldet die Reproduzierbarkeits-
// prüfung bei jedem Lauf Unterschiede. `true` tut nichts und endet erfolgreich;
// lint-staged verlangt eine nicht-leere Konfiguration.
export default { '*': 'true' }
