---
title: Tabellen
kategorie: data
status: umgesetzt
updated: 2026-09-13
---

# Tabellen

Erzeugt aus dem Drizzle-Schema mit `pnpm docs:data`. Nicht von Hand ändern.

Zurück zur [Daten-Übersicht](README.md).

## `users`

Drizzle-Export `users` in `server/database/schema/auth.ts`.

| Spalte | Typ |
| ------ | --- |

## `sessions`

Drizzle-Export `sessions` in `server/database/schema/auth.ts`.

| Spalte | Typ |
| ------ | --- |

## `accounts`

Drizzle-Export `accounts` in `server/database/schema/auth.ts`.

| Spalte | Typ |
| ------ | --- |

## `verifications`

Drizzle-Export `verifications` in `server/database/schema/auth.ts`.

| Spalte | Typ |
| ------ | --- |

## `roles`

Drizzle-Export `roles` in `server/database/schema/auth.ts`.

| Spalte | Typ |
| ------ | --- |

## `user_roles`

Drizzle-Export `userRoles` in `server/database/schema/auth.ts`.

| Spalte | Typ |
| ------ | --- |

## `role_permissions`

Drizzle-Export `rolePermissions` in `server/database/schema/auth.ts`.

| Spalte | Typ |
| ------ | --- |

## `calendar_entries`

Drizzle-Export `calendarEntries` in `server/database/schema/calendar.ts`.

| Spalte | Typ |
| ------ | --- |

## `items`

Drizzle-Export `items` in `server/database/schema/catalog.ts`.

| Spalte | Typ |
| ------ | --- |

## `item_price_versions`

Drizzle-Export `itemPriceVersions` in `server/database/schema/catalog.ts`.

| Spalte | Typ |
| ------ | --- |

## `tires`

Drizzle-Export `tires` in `server/database/schema/catalog.ts`.

| Spalte | Typ |
| ------ | --- |

## `tire_price_versions`

Drizzle-Export `tirePriceVersions` in `server/database/schema/catalog.ts`.

| Spalte | Typ |
| ------ | --- |

## `tire_photos`

Drizzle-Export `tirePhotos` in `server/database/schema/catalog.ts`.

| Spalte | Typ |
| ------ | --- |

## `sent_messages`

Drizzle-Export `sentMessages` in `server/database/schema/communication.ts`.

| Spalte | Typ |
| ------ | --- |

## `mail_templates`

Drizzle-Export `mailTemplates` in `server/database/schema/communication.ts`.

| Spalte | Typ |
| ------ | --- |

## `smtp_settings`

Drizzle-Export `smtpSettings` in `server/database/schema/communication.ts`.

| Spalte | Typ |
| ------ | --- |

## `posts`

Drizzle-Export `posts` in `server/database/schema/communication.ts`.

| Spalte | Typ |
| ------ | --- |

## `customers`

Drizzle-Export `customers` in `server/database/schema/customers.ts`.

| Spalte | Typ |
| ------ | --- |

## `suppliers`

Drizzle-Export `suppliers` in `server/database/schema/customers.ts`.

| Spalte | Typ |
| ------ | --- |

## `customer_inquiries`

Drizzle-Export `customerInquiries` in `server/database/schema/customers.ts`.

| Spalte | Typ |
| ------ | --- |

## `documents`

Drizzle-Export `documents` in `server/database/schema/documents.ts`.

| Spalte | Typ |
| ------ | --- |

## `document_items`

Drizzle-Export `documentItems` in `server/database/schema/documents.ts`.

| Spalte | Typ |
| ------ | --- |

## `document_payments`

Drizzle-Export `documentPayments` in `server/database/schema/documents.ts`.

| Spalte | Typ |
| ------ | --- |

## `document_pdfs`

Drizzle-Export `documentPdfs` in `server/database/schema/documents.ts`.

| Spalte | Typ |
| ------ | --- |

## `reminders`

Drizzle-Export `reminders` in `server/database/schema/documents.ts`.

| Spalte | Typ |
| ------ | --- |

## `reminder_pdfs`

Drizzle-Export `reminderPdfs` in `server/database/schema/documents.ts`.

| Spalte | Typ |
| ------ | --- |

## `employees`

Drizzle-Export `employees` in `server/database/schema/employees.ts`.

| Spalte | Typ |
| ------ | --- |

## `employee_absences`

Drizzle-Export `employeeAbsences` in `server/database/schema/employees.ts`.

| Spalte | Typ |
| ------ | --- |

## `employee_salary_versions`

Drizzle-Export `employeeSalaryVersions` in `server/database/schema/employees.ts`.

| Spalte | Typ |
| ------ | --- |

## `time_entries`

Drizzle-Export `timeEntries` in `server/database/schema/employees.ts`.

| Spalte | Typ |
| ------ | --- |

## `workshop_hours`

Drizzle-Export `workshopHours` in `server/database/schema/employees.ts`.

| Spalte | Typ |
| ------ | --- |

## `ebay_credentials`

Drizzle-Export `ebayCredentials` in `server/database/schema/integrations.ts`.

| Spalte | Typ |
| ------ | --- |

## `ebay_listings`

Drizzle-Export `ebayListings` in `server/database/schema/integrations.ts`.

| Spalte | Typ |
| ------ | --- |

## `ebay_import_runs`

Drizzle-Export `ebayImportRuns` in `server/database/schema/integrations.ts`.

| Spalte | Typ |
| ------ | --- |

## `access_import_jobs`

Drizzle-Export `accessImportJobs` in `server/database/schema/integrations.ts`.

| Spalte | Typ |
| ------ | --- |

## `ledger_categories`

Drizzle-Export `ledgerCategories` in `server/database/schema/ledger.ts`.

| Spalte | Typ |
| ------ | --- |

## `ledger_entries`

Drizzle-Export `ledgerEntries` in `server/database/schema/ledger.ts`.

| Spalte | Typ |
| ------ | --- |

## `work_orders`

Drizzle-Export `workOrders` in `server/database/schema/orders.ts`.

| Spalte | Typ |
| ------ | --- |

## `work_order_items`

Drizzle-Export `workOrderItems` in `server/database/schema/orders.ts`.

| Spalte | Typ |
| ------ | --- |

## `work_order_assignees`

Drizzle-Export `workOrderAssignees` in `server/database/schema/orders.ts`.

| Spalte | Typ |
| ------ | --- |

## `company_settings`

Drizzle-Export `companySettings` in `server/database/schema/settings.ts`.

| Spalte | Typ |
| ------ | --- |

## `number_ranges`

Drizzle-Export `numberRanges` in `server/database/schema/settings.ts`.

| Spalte | Typ |
| ------ | --- |

## `tire_storage`

Drizzle-Export `tireStorage` in `server/database/schema/tire-storage.ts`.

| Spalte | Typ |
| ------ | --- |

## `tire_reminder_log`

Drizzle-Export `tireReminderLog` in `server/database/schema/tire-storage.ts`.

| Spalte | Typ |
| ------ | --- |

## `vehicles`

Drizzle-Export `vehicles` in `server/database/schema/vehicles.ts`.

| Spalte | Typ |
| ------ | --- |

## `vehicle_license_plate_versions`

Drizzle-Export `vehicleLicensePlateVersions` in `server/database/schema/vehicles.ts`.

| Spalte | Typ |
| ------ | --- |

## `vehicle_purchases`

Drizzle-Export `vehiclePurchases` in `server/database/schema/vehicles.ts`.

| Spalte | Typ |
| ------ | --- |

## `vehicle_sales`

Drizzle-Export `vehicleSales` in `server/database/schema/vehicles.ts`.

| Spalte | Typ |
| ------ | --- |

## `vehicle_listings`

Drizzle-Export `vehicleListings` in `server/database/schema/vehicles.ts`.

| Spalte | Typ |
| ------ | --- |

## `vehicle_photos`

Drizzle-Export `vehiclePhotos` in `server/database/schema/vehicles.ts`.

| Spalte | Typ |
| ------ | --- |

## `vehicle_documents`

Drizzle-Export `vehicleDocuments` in `server/database/schema/vehicles.ts`.

| Spalte | Typ |
| ------ | --- |
